class AiBot { 
    constructor() {
        this.name = 'AiBot';
        this.score = 0;
        this.moves = 0;
    }
    move() {
        if (this.moves == 0){ 
            let down = Math.floor(Math.random() * 10); 
            let across = Math.floor(Math.random() * 10);
            
        }
    }
}